development:
  dialect: sqlite3
  datasource: build/data/pingplop.db
  dir: migrations
  table: _migrations

production:
  dialect: sqlite3
  datasource: build/data/pingplop.db
  dir: migrations
  table: _migrations
